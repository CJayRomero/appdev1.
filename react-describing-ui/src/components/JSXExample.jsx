export default function JSXExample() {
  // JSX looks like HTML inside JS
  return (
    <section>
      <h4>JSX Example</h4>
      <p>JSX compiles to React.createElement calls under the hood.</p>
    </section>
  );
}
